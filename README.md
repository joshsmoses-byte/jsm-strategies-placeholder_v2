# JSM Placeholder Site — V2

Separate placeholder iteration. The existing full JSM site remains untouched.

V3 changes:
- Uses the exact uploaded JSM logo asset.
- Uses Inter for all website typography.
- Removes service-category language from the top.
- Uses a clean Chicago image with the generated image's embedded text cropped out.
- Keeps the page intentionally minimal.

Deploy command:
npx wrangler deploy

Cloudflare project name:
jsm-strategies-placeholder-v3
