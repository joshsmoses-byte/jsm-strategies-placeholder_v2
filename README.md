# JSM Placeholder Site — V2

Separate placeholder iteration. This does NOT replace the full JSM website sandbox.

Changes from V1:
- Inter used for all site typography.
- Removed service-category language from the top.
- Removed all embedded text from the Chicago photograph.
- Simplified the layout.
- Kept the placeholder intentionally spare.

IMPORTANT:
The exact selected JSM logo is still required as a standalone image/SVG asset.
The File Library contains the logo concept sheet, but this build environment cannot
directly mount that library image into the repository. The current header is therefore
a temporary typographic slot and should be replaced with the actual logo before this
iteration is made public.

Suggested asset name: assets/jsm-logo.svg or assets/jsm-logo.png
